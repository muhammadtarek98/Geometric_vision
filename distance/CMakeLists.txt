file(GLOB SRC "./source/*.cpp")
file(GLOB INCLUDE "./header/*.h")
add_library(Distance STATIC ${SRC})
target_link_libraries(Distance PUBLIC Vector Line Plane Point)
target_include_directories(Distance PUBLIC ./header)